PORT=3000
BASE_URL=http://localhost:3000

# Option A: Connection URL (Railway, production)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Option B: Individual vars (local development)
DATABASE_HOST=localhost
DATABASE_NAME=ecommercedb
DATABASE_USER=your_db_user
DATABASE_PASSWORD=your_db_password
DATABASE_PORT=5432

JWT_SECRET=your-jwt-secret-here

DATABASE_ENTITIES=dist/**/*.entity.{ts,js}

ADMIN_EMAIL=admin@admin.com
ADMIN_PASSWORD=change-this-password

# AWS Configuration (leave empty for local EventEmitter mode)
AWS_REGION=us-east-1
AWS_SNS_TOPIC_ARN=
AWS_SQS_QUEUE_URL=

# Environment (controls synchronize, logging)
NODE_ENV=development
